import React from 'react';

export default function EventPage({ match, location }) {
  console.log('match', match);
  console.log('location', location);

  return (
    <div>
      <h1>Event Page</h1>
    </div>
  );
}
